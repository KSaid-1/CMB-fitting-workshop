# Jupyter Notebook activity solutions (not for students)
CAMB_Plotting_Activity.ipynb
Model_fitting_solution.ipynb
*solution*.ipynb
*Solution*.ipynb
*SOLUTION*.ipynb

# MCMC chains. These are always generated by running a notebook - never committed.
# Root-level ones come from the older solution notebooks; data/ ones come from both the
# student notebooks and the *_solution_new notebooks, so a solution chain must not be
# committable either.
/samples_*.dat
data/samples_*.dat

# Reference PDFs / course docs at the repo root (not for distribution).
# Anchored to root so figures students save into data/ are NOT ignored.
/*.pdf

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Python cache files
__pycache__/
*.py[cod]
*$py.class
*.so

# Jupyter Notebook checkpoints
.ipynb_checkpoints

# Virtual environments
venv/
env/
ENV/

# IDE files
.vscode/
.idea/
*.swp
*.swo

# Data files (if large)
*.fits
*.h5
*.hdf5

# Output plots and results
figures/
plots/
results/